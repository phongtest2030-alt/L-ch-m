package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Bảng màu thanh lịch cao cấp (Warm Ivory, Rich Lacquer Crimson & Antique Gold)
val CrimsonPrimaryLight = Color(0xFF9E1B1B)      // Đỏ chu sa hoàng gia, quý phái & tương phản cao
val CrimsonPrimaryDark = Color(0xFFFF6E6E)       // Đỏ sáng trang nhã trên nền tối

val GoldAntiqueLight = Color(0xFF8D6210)         // Vàng đồng cổ điển, đậm nét dễ đọc
val GoldAntiqueDark = Color(0xFFFFCF66)          // Vàng ánh kim dịu nhẹ trên nền tối

// Tông nền sạch, sang, tối giản: Kem ngọc trai / Trắng ngà / Xám nhạt cao cấp
val BackgroundCreamLight = Color(0xFFF9F6F0)     // Nền kem ngà tinh tế (Ivory Pearl)
val SurfaceCardLight = Color(0xFFFFFFFF)         // Thẻ trắng tinh khiết có chiều sâu
val SurfaceVariantWarm = Color(0xFFF2ECE1)       // Thẻ phụ ấm áp

val BackgroundWarmDark = Color(0xFF13110F)       // Nền đen mực xạ ấm áp
val SurfaceCardDark = Color(0xFF1C1917)          // Thẻ đen than đá cẩm thạch
val SurfaceVariantDark = Color(0xFF292524)       // Thẻ phụ tông tối

// Chữ nổi bật rõ ràng, tương phản tuyệt đối
val TextPrimaryCharcoal = Color(0xFF1C1917)      // Đen than đá ấm áp (độ tương phản 15:1)
val TextSecondaryMuted = Color(0xFF57534E)       // Xám đá graphite trung tính (độ tương phản 6.5:1)

val TextPrimaryDark = Color(0xFFFAF7F2)          // Trắng kem ngọc trai
val TextSecondaryDark = Color(0xFFA8A29E)        // Xám bạc thanh lịch

// Viền thẻ mỏng nhẹ tinh tế
val BorderSubtleLight = Color(0xFFE8E2D7)
val BorderSubtleDark = Color(0xFF332F2B)

// Màu bổ trợ trạng thái & ngày tốt
val RedBadgeLight = Color(0xFFFDF0ED)
val RedBadgeBorder = Color(0xFFF5C6BC)
val GreenHoangDao = Color(0xFF2E6E3D)
